# Session: Inman lab setup and registration

Setting up `labs/inman/` from the previously written conversion scripts
(originally authored by Neha Thomas), registering the lab in the dispatch
registries (incoming `000519`, standardized `000526`), and adding an
integration test mirroring the Kemere pattern.

## Request 1 — Set up and register

> Please setup and register the Inman group, Incoming dataset `000519`,
> standardized dataset `000526`
>
> previous work is attached, just use it as reference but improve it however
> possible

(Attached: `README.md`, `config.yaml`, `inman_to_nwb.py` from the original
conversion work.)

## Request 2 — Scope correction

> actually do not improve, just set up and add tests similar to kemere

## Request 3 — Credit

> we will do separate follow-up for improvements; ensure 'Neha Thomas' gets
> credit for the existing scripts since we can't just port over the commit
> history
