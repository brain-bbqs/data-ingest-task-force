# Session: Sanes conversion port

Porting the previously written Sanes conversion work (originally authored by Neha
Thomas) into `labs/sanes/` as-is, and registering the lab in the dispatch
registries (incoming `000522`, standardized `000523`, output written under an
`autogenerated/` subdirectory of the standardized dandiset).

## Request 1 — Port it

> Can you port over these conversion scripts as-is (will refactor in follow-up),
> similar to several of the other labs so far? And attribute it all to Neha
>
> Make the output destination nested under `/autogenerated` dir since there is
> already some previous work there
>
> The incoming dataset is: 000522
> The standardized dataset is: 000523

(Attached: `sanesDatatoNWB_v2.ipynb`, `pynaViz_ex.py`, `pynapple.yaml`,
`nwb_neuroconv_sleap.yaml`, `README.md` from the original conversion work.)

## Request 2 — Award number and attribution

Asked which grant award number the ingest name should use, and how Neha should be
credited. Answers:

> R34DA059513

> Neha Thomas
