# Session: Shepherd conversion port

Porting the previously written Shepherd conversion scripts (originally authored
by Grace Bezold) into `labs/shepherd/` verbatim, and registering the lab in the
dispatch registries (incoming `000528`, standardized `000529`).

## Request 1 — Port it

> ok now can you port over the original code for the Shepherd conversion?
>
> I've attached them - I know they need some work but don't change anything,
> leave this as original provenance and add a note at the top about how its from
> Grace originally
>
> The Shepherd incoming dataset is 000528, the standardized output is 000529

(Attached: `README.md`, `config.yaml`, `shepherd_to_nwb.py` from the original
conversion work.)
